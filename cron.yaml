cron:
# RETIRED 2026-05-30 — the Crab Crawlers bot system was already dark (last bot_run
# 2026-04-13, 47 days silent); it stayed hidden because _check_crons was crashing on
# a bad column ref. Solo-build now (no demo audience), and the bots only generated
# [BOT]/Scottsdale junk in member_watches. Handler + /live page left as inert plumbing;
# re-add this entry if the bot showcase is ever wanted again.
# - description: "Crab Crawlers — run a random bot trip + nurture past trips"
#   url: /tasks/crawl
#   schedule: every 2 hours
#   timezone: America/Los_Angeles

- description: "Refresh global deals cache from all travel APIs"
  url: /tasks/refresh-deals
  schedule: every 24 hours
  timezone: America/Los_Angeles

- description: "Evaluate watch alerts against OpenClaw-written observations (decision tier; scanning lives on the VPS)"
  url: /tasks/evaluate-alerts
  schedule: every 1 hours

- description: "Daily crab heartbeat — health check + waiting queue (one email/day to admin)"
  url: /cron/daily-heartbeat
  schedule: every day 08:00
  timezone: America/Los_Angeles

- description: "Roll raw flight observations into permanent flight_price_daily history, prune raw >30d"
  url: /cron/rollup-flight-prices
  schedule: every day 09:00
  timezone: America/Los_Angeles

# II keep-alive removed 2026-05-03 — II membership lapsed 2019, RedWeek is
# the rental path now (see docs/20260502_redweek_next_steps.md). Handler code
# left in place as inert plumbing; revive cron only if II ever comes back.

# Disabled until crab.travel is in prod — burst sends (3 emails in 6s to the
# same inbox across plans) got flagged by Gmail's spam filter. Re-enable after
# reworking to batch per-user across plans.
# - description: "Email/SMS reminders to members who haven't voted yet"
#   url: /tasks/vote-reminders
#   schedule: every day 09:00
#   timezone: America/Los_Angeles

# DISABLED 2026-04-21 — synchronous scraping on App Engine was blocking web
# traffic during crawl. Drainer moved to a local script that writes directly
# to Cloud SQL. Re-enable only if the scraper gets decoupled to Cloud Run.
# - description: "Timeshare — process next II catalog region (polite chunk)"
#   url: /tasks/ii-scrape-next
#   schedule: every 15 mins
#   timezone: America/Los_Angeles
